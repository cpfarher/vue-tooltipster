import Vue-tooltipster from './vue-tooltipster.vue';

describe('Vue-tooltipster', () => {
  it('should be a vue-tooltipster', () => {
    expect(Vue-tooltipster.name).toEqual('vue-tooltipster');
  });
});
